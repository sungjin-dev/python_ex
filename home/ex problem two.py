📝 문제 1: 다이어리 포인트 적립 장부 (기본 난이도)
글을 쓸 때마다 글자 수에 비례해서 포인트를 누적해 주는 시스템을 만들어 봅시다. 핵심은 '기존 포인트 장부'를 함수에 넣고, 계산된 '새로운 포인트 장부'를 돌려받는 것입니다.

요구사항:

calculate_point 라는 이름의 함수를 만드세요.

이 함수는 두 개의 매개변수를 받습니다.

current_point (현재까지 모인 포인트, 숫자)

diary_text (오늘 쓴 다이어리 내용, 문자열)

함수 안에서 diary_text의 글자 수(공백 포함)를 구한 뒤, 글자당 10점씩 계산하여 current_point에 더해줍니다.

최종적으로 업데이트된 총 포인트를 return 하세요.

동작 예시 (테스트 코드):

Python
my_point = 100 # 기존에 100포인트가 있는 상태
today_text = "오늘은 코딩 공부를 했다." 

# 함수 실행 후, 리턴된 값을 다시 내 장부(my_point)에 덮어씌웁니다.
my_point = calculate_point(my_point, today_text) 

print(my_point) 
# 출력 예상 결과: 240 (글자 수 14자 * 10점 = 140점 + 기존 100점)
📝 문제 2: 해시태그 자동 분류 장부 (조금 더 생각하기)
다이어리에 쓴 기분(mood)을 리스트 형태의 장부에 차곡차곡 누적하는 함수입니다. 파이썬의 리스트를 매개변수로 다루고 리턴하는 감각을 익히기 좋습니다.

요구사항:

add_mood 라는 이름의 함수를 만드세요.

이 함수는 두 개의 매개변수를 받습니다.

mood_ledger (지금까지의 기분이 담긴 리스트)

today_mood (오늘의 기분, 문자열)

함수 안에서 today_mood가 이미 mood_ledger 안에 있는지 확인합니다.

만약 없다면, 리스트의 맨 끝에 추가(append)합니다.

만약 이미 있다면, 중복해서 추가하지 않고 "이미 있는 기분입니다"라고 화면에 출력(print)만 합니다.

최종적으로 업데이트된 리스트를 return 하세요.

동작 예시 (테스트 코드):

Python
my_moods = ["행복", "우울"] # 내 기분 장부 초기 상태

# 새로운 기분 추가하기
my_moods = add_mood(my_moods, "뿌듯")
print(my_moods) # 출력 예상 결과: ["행복", "우울", "뿌듯"]

# 이미 있는 기분 다시 넣어보기
my_moods = add_mood(my_moods, "행복")
# 출력 예상: "이미 있는 기분입니다"
print(my_moods) # 출력 예상 결과: ["행복", "우울", "뿌듯"] (변화 없음)